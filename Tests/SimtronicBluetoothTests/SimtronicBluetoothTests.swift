import XCTest
@testable import SimtronicBluetooth

final class SimtronicBluetoothTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimtronicBluetooth().text, "Hello, World!")
    }
}
